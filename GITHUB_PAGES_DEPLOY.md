# V23 GitHub Pages 發布

1. 將此資料夾內容放到 GitHub repository 根目錄。
2. GitHub Desktop → Commit to main → Push / Publish。
3. GitHub → Settings → Pages。
4. Source：Deploy from a branch。
5. Branch：main。
6. Folder：/ (root)。
7. Save。

本版使用相對路徑，適合：
https://<帳號>.github.io/<repository>/

重要：
- `apps-script/` 已從公開版移除。
- 不要把 Apps Script 的 SHARED_TOKEN 或任何同步密鑰放進公開 repository。
